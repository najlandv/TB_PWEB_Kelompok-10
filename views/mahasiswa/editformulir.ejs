<% const editFormulirContent = '' %>
<% const id = user_id %>
<%- include('./template', { content: editFormulirContent, user_id: user_id}) %>
    <div class="container">
      <h1>Isi Formulir</h1>
      <form
        action="/mahasiswa/editformulir/<%= requestDetail.nomorSurat %>/update"
        method="POST"
      >
        <input type="hidden" name="id" />

        <!-- <div class="form-group">
                <label for="tanggalDikirim">Tanggal Dikirim:</label>
                <input type="date" id="tanggalDikirim" name="tanggalDikirim">
            </div>
             -->
        <!-- <div class="form-group">
                <label for="tanggalDisetujui">Tanggal Disetujui:</label>
                <input type="date" id="tanggalDisetujui" name="tanggalDisetujui">
            </div> -->

        <div class="form-group">
          <label for="penerima">Penerima:</label>
          <input
            value="<%= requestDetail?.penerima %>"
            type="text"
            id="penerima"
            name="penerima"
          />
        </div>

        <div class="form-group">
          <label for="instansi">Instansi:</label>
          <input
            value="<%= requestDetail?.instansi %>"
            type="text"
            id="instansi"
            name="instansi"
          />
        </div>

        <div class="form-group">
          <label for="judulTA">Judul TA:</label>
          <input
            value="<%= requestDetail?.judulTA %>"
            type="text"
            id="judulTA"
            name="judulTA"
          />
        </div>

        <input type="hidden" name="id_user" />

        <button type="submit">Submit</button>
      </form>
    </div>
  </body>
</html>

<script>
  function deleteConfirm() {
    if (window.confirm("apakah yakinaskdjasld")) {
      return true;
    } else {
      return false;
    }
  }
</script>
